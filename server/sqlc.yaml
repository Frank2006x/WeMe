version: "2"

sql:
  - schema: "sql/schema.sql"
    queries: "sql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "dbgen"
        out: "internal/dbgen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
